# PostgreSQL Instructions

For realistic row estimates, run `ANALYZE` or `VACUUM` before running the script.

## all.sql

Generates a report of all tables and related information including row sizes, column counts.

## summary.sql

Generates a report of all schemas, including max table size, total columns.
